/* tslint:disable object-literal-sort-keys */

const row = 3;
const column = 3;

export const height = {
  small: `${row}px`,
  medium: `${row * 2}px`,
  big: `${row * 4}px`,
};
export const width = {
  small: `${column}px`,
  medium: `${column * 2}px`,
  big: `${column * 4}px`,
};
