// import theme from "src/theme";
import { style } from "treat";

export const pixelContainer = style({
  display: "flex",
  flexDirection: "row",
  flexWrap: "wrap",
});

export const pixel = style({
  height: "100px",
  width: "100px",
});
